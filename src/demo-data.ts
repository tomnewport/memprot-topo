import type { ProteinData } from './types.js';

// Stub — run `npm run prebuild:demo` to populate with real protein data
// This file is overwritten by scripts/prebuild-demo.ts during CI and local demo builds
export const proteins: Record<string, ProteinData> = {};
